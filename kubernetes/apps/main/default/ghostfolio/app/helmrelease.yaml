---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app ghostfolio
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: app-template
  dependsOn: []
  values:
    controllers:
      ghostfolio:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: docker.io/ghostfolio/ghostfolio
              tag: 3.51.0@sha256:ed765eb3a226ce87b8ccd943ddb47e071a34b7b6c1ed4e87b448fb4563cd6ab1
            env:
              NODE_ENV: production
              PORT: &port 3333
              POSTGRES_DB: *app
              POSTGRES_USER: *app
              POSTGRES_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: postgres-ghostfolio-app
                    key: password
              DATABASE_URL:
                valueFrom:
                  secretKeyRef:
                    name: postgres-ghostfolio-app
                    key: uri
              REDIS_HOST: ghostfolio-dragonfly.default.svc.cluster.local
              REDIS_PORT: "6379"
            envFrom:
              - secretRef:
                  name: "{{ .Release.Name }}-secret"
            resources:
              requests:
                cpu: 10m
              limits:
                memory: 3500Mi
    service:
      app:
        controller: ghostfolio
        ports:
          http:
            port: *port
    route:
      app:
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Finance
          gethomepage.dev/name: Ghostfolio
          gethomepage.dev/icon: ghostfolio.png
          gethomepage.dev/description: Wealth Management
        hostnames:
          - "{{ .Release.Name }}.${SECRET_DOMAIN}"
        parentRefs:
          - name: envoy-internal
            namespace: network
