---
# yaml-language-server: $schema=https://kube-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app karakeep
  namespace: &namespace default
spec:
  targetNamespace: *namespace
  commonMetadata:
    labels:
      app.kubernetes.io/name: *app
  components:
    - ../../../../../components/kopiur/backup
    - ../../../../../components/oidc
  path: ./kubernetes/apps/main/default/karakeep/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  wait: false # no flux ks dependents
  interval: 30m
  retryInterval: 1m
  timeout: 5m
  postBuild:
    substitute:
      APP: *app
      NAMESPACE: *namespace
      APP_NAMESPACE: *namespace
      KOPIUR_SCHEDULE: "7 * * * *"
      KOPIUR_CAPACITY: 5Gi
      OIDC_DISPLAY_NAME: Karakeep
      OIDC_CALLBACK: /api/auth/callback/custom
      OIDC_SHORT_USERNAME: "true"
      OIDC_LEGACY_CRYPTO: "true"
      OIDC_CLIENT_ID_KEY: OAUTH_CLIENT_ID
      OIDC_CLIENT_SECRET_KEY: OAUTH_CLIENT_SECRET
