---
kind: Service
apiVersion: v1
metadata:
  name: mc
  namespace: games
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "mc.${SECRET_DOMAIN}"
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"
spec:
  type: ExternalName
  externalName: mc.${SECRET_DOMAIN}