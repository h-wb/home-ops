---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: radarr-uhd
  namespace: media
spec:
  target:
    deletionPolicy: Delete
    template:
      type: Opaque
      data:
        RADARR__API_KEY: "{{ .RADARR__API_KEY }}"
  dataFrom:
    - extract:
        key: radarr
