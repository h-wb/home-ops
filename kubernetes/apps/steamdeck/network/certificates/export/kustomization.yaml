---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./certificate.yaml
  # pushsecret.yaml is intentionally disabled: steamdeck imports the wildcard cert from BWS
  # (issued and pushed by the main cluster). Only enable this if steamdeck ever becomes the
  # primary cert issuer and main should import from it instead.
  # - ./pushsecret.yaml
